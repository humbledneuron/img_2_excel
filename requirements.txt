pip install -r requirements.txt

# for linux if any issue with pip
# pipx install pytessearct
# pipx install pandas
# pipx install numpy
# pipx install pillow
# pipx install openpyxl



# for virtual environment

python3 -m venv myenv
source myenv/bin/activate

pip install pytesseract
pip install pandas
pip install numpy
pip install pillow
pip install openpyxl
pip install pdf2image


## extra features
# pdf to image to excel
#every 
# in the all amount | MONTO should be calculated 
# formula for amount calculation = 
#=SUM(VALUE(TRIM(SUBSTITUTE(b4,"$",""))),VALUE(TRIM(SUBSTITUTE(b2,"$",""))))
# seggegate the pdf's based on the bank name #yet to be implemented